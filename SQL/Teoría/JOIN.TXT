SELECT CampoT1, CampoT2
FROM Tabla1 T1 JOIN Tabla2 CampoT2
ON T1CampoComun1 = T2CampoComun1 
WHERE T1.Campo1 > T2.Campo2 (Se utilizan los alias)
GROUP BY ...
HAVING ...


Si hay campos nulos en la tabla de la izquierda, entonces se hace un left join.
Si hay campos nulos en la tabla de la derecha , entonces se hace un right join.